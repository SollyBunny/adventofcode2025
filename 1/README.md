A circular dial between 0 and 99 which initally points at 50

Rotations are encoded as L or R followed by a number

There is a decoy password containing instructions as above

The actual password is the number of times the dial is at 0 when executing the instructions
